#ifndef __ARITHMETIC_H__
#define __ARITHMETIC_H__

int isprime(int n); /* return 1 if n is prime, otherwise return 0 */
int gcd(int x, int y); /* return the greatest common divisor of x and y */
int lcm(int x, int y); /* return the least common multiplier of x and y */

#endif

